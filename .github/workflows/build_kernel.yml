name: Download and Execute Script

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v2

    - name: Display current directory and list contents (after checkout)
      run: |
        pwd
        ls -l

    - name: Install aria2
      run: sudo apt-get install -y aria2

    - name: Download file
      run: |
        aria2c -x16 https://fireos-tablet-src.s3.amazonaws.com/9OXq0RSjrgQuqrJlQ8itQTSO4r/Fire_HD8_10th_Gen-7.3.2.9-20231103.tar.bz2 -o Fire_HD8_10th_Gen-7.3.2.9-20231103.tar.bz2
        pwd
        ls -l

    - name: Extract tar.bz2 file
      run: |
        tar -xjf Fire_HD8_10th_Gen-7.3.2.9-20231103.tar.bz2
        pwd
        ls -l

    - name: Extract build_kernel.tar.gz
      run: |
        tar -xzf build_kernel.tar.gz
        pwd
        ls -l

    - name: Setup clang 7.0.0
      run: |
        echo  "Downloading clang 7.0.0"
        # Set up variables
        CLANG_VERSION="7.0.0"
        CLANG_DIR="prebuilt"
        CLANG_TAR="clang+llvm-${CLANG_VERSION}-x86_64-linux-gnu-ubuntu-16.04.tar.xz"
        CLANG_URL="http://releases.llvm.org/${CLANG_VERSION}/${CLANG_TAR}"
        CLANG_EXTRACT_DIR="clang+llvm-${CLANG_VERSION}-x86_64-linux-gnu-ubuntu-16.04"
        CLANG_BIN_DIR="${CLANG_EXTRACT_DIR}/bin"
        sed -i 's|CLANG_COMPILER_PATH=""|CLANG_COMPILER_PATH='"$(pwd)"'/'"${CLANG_EXTRACT_DIR}"'|' build_kernel_config.sh
        # Create necessary directories
        
        mkdir -p ${CLANG_BIN_DIR}
    
        # Download and extract clang
        echo "Downloading clang ${CLANG_VERSION}"
        aria2c -x16 ${CLANG_URL}
        tar -xf ${CLANG_TAR}
        ls -l
        ls -l clang*
        
        

        
    - name: Run build_kernel.sh
      run: |
        pwd
        ls -l
        ./build_kernel.sh platform.tar output
        
